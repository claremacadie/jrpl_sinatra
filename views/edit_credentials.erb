<h1>Administer your account</h1>
<form method="post" action="/user/edit_credentials">
  <div>
    <label for="password"> Enter current password:
      <input type="password" id="password" name="current_password" />
    </label>
  </div>
  <div>
    <label for="new_username"> Enter new username:
      <input name="new_username" id="new_username" value="<%== session[:user_name] %>"/>*Leave unchanged if you don't wish to alter your username.
    </label>
  </div>
  <div>
    <label for="new_firstname"> Enter your first name:
      <input name="new_firstname" id="new_firstname" value="<%= params[:new_firstname] %>"/>*Leave unchanged if you don't wish to alter your first name.
    </label>
  </div>
  <div>
    <label for="new_lastname"> Enter your last name:
      <input name="new_lastname" id="new_lastname" value="<%= params[:new_lastname] %>"/>*Leave unchanged if you don't wish to alter your last name.
    </label>
  </div>
  <div>
    <label for="new_displayname"> Enter your display name:
      <input name="new_displayname" id="new_displayname" value="<%= params[:new_displayname] %>"/>*Leave unchanged if you don't wish to alter your display name.
    </label>
  </div>
  <div>
    <label for="password"> Enter new password:
      <input type="password" id="password" name="new_password" />*Leave blank if you don't wish to alter your password.
    </label>
  </div>
  <div>
    <label for="reenter_password"> Reenter new password:
      <input type="password" id="reenter_password" name="reenter_password" />*Leave blank if you don't wish to alter your password.
    </label>
  </div>
  <button type="submit">Change account details</button>
  <a href='/'>Cancel</a>
</form>